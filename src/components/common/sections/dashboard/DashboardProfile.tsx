import { FC } from "react";

const DashboardProfile: FC = (): JSX.Element => {
  return (
    <div className="dashboard-home__profile sticky top-0 w-[350px] bg-white h-[500px]"></div>
  );
};

export default DashboardProfile;
