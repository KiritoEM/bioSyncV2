/**
 * Section hero du landing
 * @returns {JSX.Element}
 */

import { FC } from "react";

const LandingHero: FC = (): JSX.Element => {
  return (
    <section className="landing__hero bg-primary h-screen">
      <div className="container mx-auto"></div>
    </section>
  );
};

export default LandingHero;
